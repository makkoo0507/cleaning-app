# 作業指示書

**プロジェクト名:** 民泊清掃管理アプリ  
**作成日:** 2026-06-20  
**バージョン:** 1.1

---

## 1. 技術スタック

| 層 | 技術 | 選定理由 |
| --- | --- | --- |
| フロントエンド（管理 Web） | Next.js (App Router) | SSR・API Routes を同一リポジトリで管理できる |
| フロントエンド（LINE Mini App） | Next.js + LIFF SDK | 管理 Web と同一フレームワーク・同一リポジトリで開発可能 |
| バックエンド / DB | Supabase | PostgreSQL、認証、リアルタイム購読、ストレージをオールインワンで提供。小規模向けの無料枠あり |
| 認証（業者） | Supabase Auth (Email / Password) | — |
| 認証（清掃者・オーナー） | LINE Login (LIFF) → Supabase セッション発行 | — |
| LINE 通知 | LINE Messaging API (Push Message) | — |
| スケジューラー（リマインド） | Supabase Edge Functions + pg_cron | 前日リマインドを cron で実行 |
| ホスティング | Vercel | Next.js との親和性が高く、無料枠で小規模運用可能 |

---

## 2. リポジトリ構成

```text
cleaning-app/
├── app/                        # Next.js App Router
│   ├── (admin)/                # 業者向け管理画面（ログイン必須）
│   │   ├── dashboard/
│   │   ├── schedules/
│   │   ├── properties/
│   │   ├── cleaners/
│   │   ├── owners/
│   │   ├── records/
│   │   └── billing/
│   ├── liff/                   # LINE Mini App ルート
│   │   ├── cleaner/            # 清掃者向け
│   │   └── owner/              # オーナー向け
│   └── api/                    # API Routes
│       ├── schedules/
│       ├── records/
│       ├── line/
│       │   └── webhook/        # LINE Webhook 受信
│       └── notifications/
├── components/
├── lib/
│   ├── supabase.ts
│   ├── line.ts                 # LINE Messaging API クライアント
│   └── liff.ts                 # LIFF 初期化ユーティリティ
├── supabase/
│   ├── migrations/             # DB マイグレーション SQL
│   └── functions/              # Edge Functions（リマインダー cron）
└── ...
```

---

## 3. データベース設計

詳細は [テーブル設計.md](テーブル設計.md) を参照。

---

## 4. 開発フェーズ

### Phase 1: 基盤構築

- [ ] リポジトリ作成（GitHub）
- [ ] Next.js プロジェクト初期化
- [ ] Supabase プロジェクト作成・DB マイグレーション実行（`contractor_companies` 含む）
- [ ] Supabase Auth 設定（業者用メール認証）
- [ ] `users.company_id` を JWT カスタムクレームに付与する設定（Auth Hook）
- [ ] RLS ポリシー設定（全テーブルで `company_id` によるテナント分離）
- [ ] LINE 公式アカウント作成・Messaging API チャネル設定
- [ ] LIFF アプリ作成（清掃者用・オーナー用 各1つ）
- [ ] Vercel デプロイ設定・環境変数設定

### Phase 2: 管理 Web アプリ

- [ ] ログイン画面
- [ ] ダッシュボード（案件サマリ）
- [ ] 物件管理 CRUD
- [ ] 清掃者管理 CRUD
- [ ] オーナー管理 CRUD
- [ ] スケジュール管理（作成・編集・削除・一覧）
- [ ] 請求・支払い額の記録・集計・CSV エクスポート
- [ ] 清掃記録一覧

### Phase 3: LINE Mini App

- [ ] LIFF 初期化・LINE ログイン処理
- [ ] Supabase セッション発行（LINE ユーザー ID → Supabase ユーザー紐付け）
- [ ] 清掃者向け: マイスケジュール画面
- [ ] 清掃者向け: 案件詳細画面
- [ ] 清掃者向け: 清掃記録登録画面
- [ ] オーナー向け: スケジュール確認画面
- [ ] オーナー向け: 清掃記録閲覧画面

### Phase 4: LINE 通知

- [ ] スケジュール作成時の通知（清掃者・オーナー）
- [ ] 清掃完了時の通知（オーナー・業者）
- [ ] 前日リマインド（Supabase Edge Function + pg_cron）

### Phase 5: テスト・リリース

- [ ] E2E フロー確認（業者 → スケジュール作成 → 清掃者通知 → 記録登録 → オーナー通知）
- [ ] 実機での LINE Mini App 動作確認
- [ ] 本番環境へのデプロイ

---

## 5. 環境変数（.env.local）

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# LINE Messaging API
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# LIFF
NEXT_PUBLIC_LIFF_ID_CLEANER=
NEXT_PUBLIC_LIFF_ID_OWNER=
```

---

## 6. LINE 通知実装メモ

### Push Message 送信（サーバーサイド）

```typescript
// lib/line.ts
import axios from 'axios';

export async function pushMessage(lineUserId: string, text: string) {
  await axios.post(
    'https://api.line.me/v2/bot/message/push',
    {
      to: lineUserId,
      messages: [{ type: 'text', text }],
    },
    {
      headers: {
        Authorization: `Bearer ${process.env.LINE_CHANNEL_ACCESS_TOKEN}`,
      },
    }
  );
}
```

### LIFF 初期化（クライアントサイド）

```typescript
// lib/liff.ts
import liff from '@line/liff';

export async function initLiff(liffId: string) {
  await liff.init({ liffId });
  if (!liff.isLoggedIn()) {
    liff.login();
  }
  const profile = await liff.getProfile();
  return profile; // { userId, displayName, pictureUrl }
}
```

---

## 7. Supabase RLS ポリシー方針

**大原則: 全ポリシーに `company_id = (自分の company_id)` の条件を必ず付加し、テナント間のデータ漏洩を防ぐ。**

| テーブル | 業者管理者 | 業者社員 | 清掃者 | オーナー |
| --- | --- | --- | --- | --- |
| contractor_companies | 自社のみ読取 | 自社のみ読取 | アクセス不可 | アクセス不可 |
| properties | 自社内 全 CRUD | 自社内 全読取・作成・編集 | 自社内 自案件の物件のみ読取 | 自社内 自物件のみ読取 |
| users | 自社内 全 CRUD | 自分のみ読取・更新 | 自分のみ読取・更新 | 自分のみ読取・更新 |
| jobs | 自社内 全 CRUD | 自社内 全読取・作成・編集 | 自社内 自分がアサインされた案件のみ読取 | 自社内 自物件の案件のみ読取 |
| cleaning_records | 自社内 全 CRUD | 自社内 全読取 | 自社内 自分の案件への書込・読取 | 自社内 自物件の案件の読取のみ |

---

## 8. 今後の拡張検討事項（優先度低）

- 民泊予約システム（Airbnb / Booking.com）との API 連携によるスケジュール自動取得
- 清掃前後の写真登録機能
- 清掃チェックリスト機能
- 業者向けモバイル対応
- 有料プランのオンライン決済導入（Stripe 等）
